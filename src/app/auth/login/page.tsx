import LoginView from "@/components/auth/LoginView";

export const metadata = {
  title: "Sign In — LogiQ-On Tech Platform",
  description: "Unified multi-tenant login portal for Platform Owners, Vendors, and Warehouse Operators.",
};

export default function LoginPage() {
  return <LoginView />;
}
