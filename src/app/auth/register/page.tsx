import RegisterView from "@/components/auth/RegisterView";

export const metadata = {
  title: "Register Account — LogiQ-On Tech Platform",
  description: "Provision a new profile on the LogiQ-On platform.",
};

export default function RegisterPage() {
  return <RegisterView />;
}
